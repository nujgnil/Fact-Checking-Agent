###############################################
# Core ML + Data
###############################################
pandas>=2.1.0
numpy>=1.26.0,<2.0    # Many ML libs break on NumPy 2.x
scikit-learn>=1.3.0
joblib>=1.3.0
tqdm>=4.66.0
matplotlib>=3.8.0

###############################################
# NLP + Embeddings + LLM
###############################################
sentence-transformers>=2.3.0
transformers>=4.36.0
accelerate>=0.25.0
tokenizers>=0.15.0
protobuf<=3.20.3   # prevents transformer serialization issues

###############################################
# Torch (Required for embeddings + Whisper)
###############################################
torch>=2.1.0

###############################################
# Retrieval (FAISS)
###############################################
faiss-cpu>=1.7.4      # CPU version (works on Windows, Mac, Linux)

###############################################
# Speech-to-Text (Whisper)
###############################################
openai-whisper>=20231117
ffmpeg-python>=0.2.0
soundfile>=0.12.1
# NOTE: You must install FFmpeg on system path (not via pip)

###############################################
# Web UI (Streamlit)
###############################################
streamlit>=1.29.0

###############################################
# LLM Clients (OpenAI / Groq)
###############################################
openai>=1.12.0
groq>=0.3.0

###############################################
# Utilities + Config + Logging
###############################################
python-dotenv>=1.0.0
pyyaml>=6.0.1
requests>=2.31.0
datasets>=2.16.0  # optional: for loading LIAR/LIAR2 if using HF format

###############################################
# Development Tools
###############################################
jupyter
ipykernel
black
flake8
